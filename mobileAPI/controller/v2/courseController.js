var pyCarriers = require(__mobileAPIBase + 'module/pyCarriers');
var tokenHelper = require( __mobileAPIBase + 'module/token');

module.exports.getCourse = function(req, res, next) {
  if (!req.body.token || !req.body.list) {
    
  }
}
